// assets/js/main/index.js
import { cargarFooter } from '../components/footer';

// Llamar a la función para cargar el footer al cargar la página
document.addEventListener('DOMContentLoaded', () => {
    cargarFooter();
});
